# 2026-03-14 — Calcifer Session Log (docs deploy + v0.2.0 prep)

**Session:** cron cofounder-sync-check, 01:54 UTC  
**Commit:** `feat(docs): GitHub Pages deploy workflow + VitePress base fix`  
**Tests:** 342 (unchanged — all passing)

---

## What happened

### Sync check
- Pulled `git log --oneline -5`: no new commits from GLaDOS
- All activity still Calcifer-authored (budget tests, Phase 4f docs)
- GLaDOS still has open validation items (boot chain, /capabilities endpoint, watch end-to-end)

### Docs deploy (Phase 4d)

The VitePress docs site has existed since Phase 4 but had no GitHub Actions workflow to
actually deploy it. Added:

**`.github/workflows/docs.yml`**
- Triggers on push to `main`/`master` when `docs-site/**` changes (+ `workflow_dispatch`)
- `concurrency: group: pages, cancel-in-progress: true` — new pushes cancel in-flight deploys
- Two jobs: `build` (install + `pnpm build` in `docs-site/`) and `deploy` (Pages artifact)
- Passes `VITE_DOCS_BASE=/cofounder/` so assets resolve correctly on GitHub Pages
- If Nic sets a custom domain (CNAME), just set `VITE_DOCS_BASE=/` in the workflow env

**`docs-site/.vitepress/config.ts`**
- `base` now reads from `process.env.VITE_DOCS_BASE ?? "/"` so local dev still works at `/`

**CHANGELOG.md** — added `[Unreleased] — v0.2.0` section with these entries

**ROADMAP.md** — checked off GitHub Pages deploy under 4d

---

## GLaDOS pending asks (unchanged)

1. **Boot chain end-to-end** (Phase 2b) — WOL → boot → gateway → cofounder watch → H1 confirms
2. **`/capabilities` endpoint** (Phase 3b) — confirm cofounder watch --serve-capabilities on real Windows
3. **`cofounder watch` + `cofounder result` end-to-end** (Phase 5c) — full dispatch loop on Windows

---

## Up next (Calcifer)

- Bump package versions to `0.2.0` once Nic gives the go-ahead for the release tag
- Phase 4d: Discord community still needs Nic's direction (name, invite link)
- Phase 6: research-blocked — waiting on Vision Wormhole / LatentMAS codec open-source
